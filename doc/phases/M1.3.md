# M1.3 晨报数据源接入（本地）

目标：明确晨报使用的数据源，并将读取逻辑从报告层抽象到数据源层。

## 新增能力
- 晨报数据源连接器：`app/connectors/brief_data_source.py`
- 报告层统一通过数据源获取结构化数据

## 出口（可验证）
- `GET /reports/morning-brief/data` 返回数据源内容
- 数据源可被替换为外部接口而不影响报告层
